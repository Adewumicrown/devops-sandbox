FROM python:3.11-slim
WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn[standard] python-dotenv
COPY platform/ ./platform/
EXPOSE 8000
CMD ["uvicorn", "platform.api:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
